package com.example.demo.auth;

public enum Role {
    // Role for administrators with full access
    ADMIN,

    // Role for regular users with standard access
    USER,

    // Role for guest users with limited access
    GUEST
}
